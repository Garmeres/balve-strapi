{
  "collectionName": "components_content_membership_registration_forms",
  "info": {
    "displayName": "Membership registration",
    "icon": "user"
  },
  "options": {},
  "attributes": {
    "lang": {
      "type": "enumeration",
      "required": true,
      "default": "no",
      "enum": [
        "no",
        "en",
        "se"
      ]
    },
    "iframeSrc": {
      "type": "string",
      "required": true
    },
    "termsAndConditions": {
      "type": "blocks",
      "required": false
    }
  },
  "config": {}
}
